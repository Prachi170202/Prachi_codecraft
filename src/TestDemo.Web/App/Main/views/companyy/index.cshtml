<div class="row clearfix" ng-controller="app.views.companyy.index as vm">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card main-content">
            <div class="header">
                <h2>
                    @L("Company")
                </h2>
                <i class="fa fa-spin fa-spinner" ng-if="vm.isTableLoading"></i>
                <ul class="header-dropdown m-r--5">
                    <li class="dropdown">
                        <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="material-icons">more_vert</i>
                        </a>
                        <ul class="dropdown-menu pull-right">
                            <li><a href="javascript:void(0);" class="waves-effect waves-block()" ng-click="vm.refresh();"><i class="material-icons">refresh</i> @L("Refresh")</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
            <div class="body table-responsive">
                <div class="form-group">
                    <label for="companyDropdown">Select Company:</label>
                    <select id="companyDropdown" class="form-control" ng-model="selectedCompany" ng-options="company.name for company in vm.companies">
                        <option value="">-- Select Company --</option>
                    </select>
                </div>
            </div>
        </div>
    </div>
</div>

